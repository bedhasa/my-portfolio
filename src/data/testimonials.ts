// src/data/testimonials.ts
// Testimonials are intentionally empty until real submissions are received.
// To add a testimonial, update this array and redeploy.

export interface Testimonial {
  name: string;
  content: string;
  date: string;
}

export const testimonials: Testimonial[] = [];
